{
  "name": "mail-watch",
  "version": "0.1.0",
  "private": true,
  "description": "代表宛メール見落とし防止AI秘書（Cloudflare Workers + D1）",
  "scripts": {
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply mail-watch-db --local",
    "db:tables:local": "wrangler d1 execute mail-watch-db --local --command \"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.20.0"
  }
}
