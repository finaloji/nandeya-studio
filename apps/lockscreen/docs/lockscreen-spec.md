# ロック画面マジックアプリ 製品仕様書 v1.0

**アプリ名（内部名）**: lockscreen
**配置先**: `apps/lockscreen/lockscreen.html`（1ファイル完結）
**ホスティング**: `https://finaloji.github.io/nandeya-studio/apps/lockscreen/lockscreen.html`

---

## マジックの全体像

「失敗→的中型」の単独演目。開錠そのものがクライマックス。

| 誰が | 何をするか |
|------|-----------|
| マジシャン | 隠しジェスチャーで演出をONにしてから端末を観客に渡す |
| 観客 | 好きなパスコードを入力する → 何を打っても「間違い」（シェイク演出） |
| 観客/マジシャン | 緊張感が高まったところで再度入力 |
| 端末 | 設定時間（デフォルト30秒）経過後は、どんな数字でも開錠 → ホーム画面が表示される |

- 数字のフォースは行わない。「ロックされていたのに開いた」という不可能性が効果の核
- 開錠後はホーム画面（ダミーアイコンの並ぶ画面）が見えて演目終了

---

## 画面構成

### 1. ロック画面（パスコード入力）

iOSの実機パスコード画面に忠実に寄せる。
※実機のパスコード入力画面には時計・日付は表示されないため、このアプリでも表示しない。

- 上部: 「パスコードを入力」の文言
- その下: 入力ドット（桁数ぶんの○。入力で塗りつぶし）
- テンキー: 1〜9・0 の円形ボタン（2〜9にはABC等の英字添え字）
- 下部: 「緊急」（左）「キャンセル」（右）のテキストラベル
- 背景: 暗めの壁紙にすりガラス風のぼかし

### 2. ホーム画面（開錠後）

- 壁紙＋ダミーアプリアイコンのグリッド＋下部Dock
- 触っても何も起きない静的な画面でよい（クライマックスの数秒間見えれば十分）

---

## パスコード入力の挙動

- タップで左からドットが埋まる
- 全桁入力された時点で自動判定（iOSと同じく決定ボタンなし）
- **不正解時**: ドット列が左右にシェイク → 全ドットが空に戻る（バイブ相当の演出）
- **正解（開錠）時**: 開錠アニメーション → ホーム画面へ遷移

### 判定ロジック

| 状態 | 判定 |
|------|------|
| 演出OFF | 常に不正解（何を打っても開かない） |
| 演出ON・開始から設定時間内 | 常に不正解 |
| 演出ON・設定時間経過後 | 常に正解（どんな数字でも開錠） |

- 開錠したら演出は自動的にOFFに戻る

---

## 隠し操作

### 演出ON/OFF（機能起動コマンド）

**「緊急」ラベルを3秒長押し**（スプリント2-1で確定） → 演出ON（タイマー開始）/ 再度3秒長押しでOFF

- 押し始めが「緊急」上のものだけ有効。途中で離す・指がずれると不成立（画面上は何も起きない）
- 1回の押下で1トグルのみ。成立の瞬間に切り替わる（指を離すのを待たない）
- **ONサイン**: ONの間だけ「緊急」の文字がごくわずかに太くなる（「キャンセル」と見比べてようやく分かる程度）
- 運用注意: 観客が偶然3秒長押しするとOFFに切り替わるため、クライマックス前にONサインを目視確認すること

- 端末を渡す直前に自然に仕込める
- ONサイン（術者だけが分かる変化）を1箇所だけ用意する（例: 文言のフォントウェイトが僅かに変わる等。詳細はスプリントで決定）

### 入力履歴の確認

**画面を2本指で上にスワイプ** → 画面下部に入力履歴パネルを表示

- これまでの全試行を時系列リストで表示（試行番号＋入力された数字）
- パネル外タップ or 2本指下スワイプで閉じる
- 履歴は演出ONのタイミングでリセット

### 隠し設定

設定できる項目:

| 項目 | 選択肢 | デフォルト |
|------|--------|-----------|
| パスコード桁数 | 4桁 / 6桁 | 6桁 |
| ロック解除までの時間 | 秒数指定 | 30秒 |

- 設定への入り方も隠し操作にする（詳細はスプリントで決定）
- 設定は端末に保存され、再読み込み後も維持される

---

## データの保存

- 桁数・ロック時間の設定は保存する
- 演出ON状態と開始時刻も保存する（演技中にページが再読み込みされても30秒カウントが壊れないように）
- 入力履歴はセッション中のみ保持でよい

---

## 難読化方針（最重要）

stopwatch / calc と同じ方針。コードを見ても「時間経過で必ず開く」仕掛けが分からないようにする。

- localStorage キーは意味のない名前
- 関数名・変数名も難読化
- コメントでは「較正」「表示終了処理」などの言葉を使い、マジック用語を書かない

### 難読化対応表（スプリント2-1時点）

| 種別 | 名前 | 実際の意味 |
|------|------|-----------|
| localStorage | `_vx1` | 演出ONフラグ（'1'） |
| localStorage | `_vx2` | ONにした瞬間のepochミリ秒 |
| localStorage | `_vx3` | 開錠可能までの秒数（未設定なら30） |
| 変数 | `_vm` / `_vt` | 演出モードフラグ / タイマー起点時刻 |
| 変数 | `_dz` | 開錠確定後の全入力無効フラグ |
| 変数 | `_hp` / `_hk` | 長押しタイマーID / 追跡中タッチID |
| 変数 | `_b` / `_q1` | 入力バッファ / 入力履歴（試行の配列） |
| 関数 | `_gd()` | 開錠可能秒数の取得 |
| 関数 | `_vs(_o)` | 演出ON/OFF切替 |
| 関数 | `_h0()` / `_h1()` | 長押し計時の開始 / 破棄 |
| 関数 | `_fo()` | 開錠演出の開始（ロック層退場＋ホーム画面出現） |
| CSSクラス | `.cw` | ONサイン（「緊急」の字を太らせる） |
| CSSクラス | `body.zz` | 開錠後の状態（ロック層が上へ抜け、ホーム画面が出現） |
| CSSクラス | `.pane2` | 偽ホーム画面の層 |
| CSSクラス | `.grid-b` / `.gb-c` / `.gb-t` / `.gb-l` | アイコングリッド / セル / アイコン面 / ラベル |
| CSSクラス | `.pill-b` | 検索ピル |
| CSSクラス | `.bar-b` | Dock（すりガラスパネル） |
| CSSクラス | `.m1`〜`.m20` / `.n1`〜`.n3` | 各ダミーアイコンの地色 |
| CSS変数 | `--td` | ホーム画面アイコンの寸法 |
| コメント語 | 「較正」「表示終了処理」「二次画面」 | 演出モード / 開錠演出 / 偽ホーム画面 |

---

## スマホ最適化

- `touchstart` / `touchend` でボタン応答（click遅延を避ける）
- `user-select: none`、`-webkit-tap-highlight-color: transparent`、`touch-action: manipulation`
- viewport: `width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no`
- PWA対応: manifest.json、apple-touch-icon、ステータスバー黒
- 縦向き前提
